import Logo from "components/Logo";
import { HeaderWrapper } from "./HeaderStyle";

const Header = () => {
    return (
        <HeaderWrapper>
            <Logo />
        </HeaderWrapper>
    );
};

export default Header;
